query UserProfile($username: Username!) {
  ...SharedLayout_Query
  userByUsername(username: $username) {
    ...UserProfileFields
  }
}

fragment UserProfileFields on User {
  ...SharedLayout_User
  bio
  createdAt
  posts(first: 10) {
    nodes {
      ...PostFields
    }
  }
}
